name: Build Universal Parent + Child APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'android/**'
      - '.github/workflows/build-android.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 36
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'

      - name: Build Universal Parent + Child APK
        run: gradle :parentapp:assembleDebug --stacktrace

      - name: Verify Universal APK exists
        run: test -f parentapp/build/outputs/apk/debug/parentapp-debug.apk

      - name: Upload Universal Parent + Child APK
        uses: actions/upload-artifact@v4
        with:
          name: bedtime-universal-apk
          path: android/parentapp/build/outputs/apk/debug/parentapp-debug.apk
