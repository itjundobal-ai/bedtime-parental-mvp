name: Build Universal Parent Child APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'android/parentapp/**'
      - 'android/childapp/src/main/java/com/master/bedtime/child/**'
      - 'android/childapp/src/main/res/layout/**'
      - 'android/childapp/src/main/res/xml/**'
      - 'android/settings.gradle.kts'
      - '.github/workflows/build-parent-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android
    steps:
      - uses: actions/checkout@v4
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
      - name: Install Android SDK 36
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'
      - name: Build universal Parent + Child APK
        run: gradle :parentapp:assembleDebug --stacktrace
      - name: Verify universal APK exists
        run: test -s parentapp/build/outputs/apk/debug/parentapp-debug.apk
      - name: Upload universal APK
        uses: actions/upload-artifact@v4
        with:
          name: bedtime-parent-new-apk
          path: android/parentapp/build/outputs/apk/debug/parentapp-debug.apk
